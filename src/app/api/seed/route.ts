import { NextResponse } from 'next/server'

export async function POST() {
  return NextResponse.json({ message: 'use prisma seed instead' })
}

export async function GET() {
  return NextResponse.json({ message: 'use prisma seed instead' })
}
